# api

Backend Elysia para DVisual ejecutado con Node.js + npm.

## Scripts

```bash
npm run dev -w api
npm run build -w api
npm run start -w api
```

Variables de entorno:

- `PORT` (default `3000`)
- `GROQ_API_KEY`
