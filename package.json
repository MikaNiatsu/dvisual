{
  "name": "dvisual",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cd web && pnpm install && pnpm run dev",
    "dev:api": "cd api && pnpm install && pnpm run dev",
    "dev:web": "cd web && pnpm install && pnpm run dev",
    "build": "cd api && pnpm install && pnpm run build && cd ../web && pnpm install && pnpm run build",
    "build:api": "cd api && pnpm install && pnpm run build",
    "build:web": "cd web && pnpm install && pnpm run build",
    "start": "cd api && pnpm install && pnpm run start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "@vitejs/plugin-react": "^5.1.4",
    "vite": "^7.3.1"
  }
}
